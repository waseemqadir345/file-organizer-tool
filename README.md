# file-organizer-tool
A Python automation tool that **organizes hundreds of messy files in seconds**. It sorts files into folders by type, batch-renames them with custom rules, and removes duplicate files automatically.
